export const getTransferAmountInitialValues = (amount) => {
  return {
    amount: amount || "",
    pin: "",
  };
};

export const defaultTransferAmountValues = {
  amount: "",
  pin: "",
};

export const defaultAmountValues = {
  amount: "",
};
